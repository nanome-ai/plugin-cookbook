# For some reason installing this environment fails. Figure out why.
name: base
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - pip
  - rdkit
  - pip:
      - nanome==0.34.3
      - jupyterlab
      - dill
      - redis
      - py2neo
      - ipdb
